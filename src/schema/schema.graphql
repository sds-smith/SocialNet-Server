type Query {
  greeting: String
  messages: [Message!]
}

type Mutation {
  addMessage(text: String!): Message!
}

type Subscription {
  messageAdded: Message
}

type Message {
  id: ID!
  user: String!
  text: String!
  createdAt: String
}
